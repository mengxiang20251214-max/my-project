# 应用密钥（生产环境必须修改）
SECRET_KEY=your-super-secret-key-change-in-production-min-32-chars

# JWT 配置
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# 数据库（默认 SQLite）
DATABASE_URL=sqlite:///./blog_video.db

# 网站配置
SITE_NAME=VideoHub Pro
SITE_DESCRIPTION=专业视频博客平台
SITE_URL=http://localhost:8000

# 前台右侧「下载 APP / 联系客服」按钮（每组 2 个，留空则不显示该按钮）
# 下载 APP（自动识别 Google Play / App Store 图标，紫色按钮）
APP_DOWNLOAD_URL_1=
APP_DOWNLOAD_NAME_1=Google Play
APP_DOWNLOAD_URL_2=
APP_DOWNLOAD_NAME_2=App Store
# 客服（自动识别 WhatsApp 绿 / Telegram 蓝）
CONTACT_URL_1=
CONTACT_NAME_1=WhatsApp
CONTACT_URL_2=
CONTACT_NAME_2=Telegram
# 旧单值变量（向后兼容，可不填）
APP_DOWNLOAD_URL=
CONTACT_URL=

# 文件上传配置
UPLOAD_DIR=../static/uploads
MAX_UPLOAD_SIZE=524288000

# ── 存储后端：local（本地磁盘，默认） | r2（Cloudflare R2 对象存储）──────────────
STORAGE_BACKEND=local
# 切到 r2 时填下面这几项（在 Cloudflare R2 控制台获取）：
R2_BUCKET=video-blog-storage
R2_ENDPOINT=https://9692ca2edc2f9abcbacf0a7a6ba218ab.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://pub-10c86c1e92b3432284fa82865c1edc1f.r2.dev
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_REGION=auto

# ── 数据库自动备份（PostgreSQL → R2 的 backups/ 目录）──────────────────────────
BACKUP_ENABLED=false          # 生产设 true 开启每日自动备份
BACKUP_RETENTION_DAYS=30      # 只保留最近 N 天的备份，更早的自动删除
BACKUP_SCHEDULE=0 2 * * *     # cron 表达式：每天 02:00 执行
BACKUP_TZ=UTC                 # cron 的时区（Railway 容器默认 UTC；改成 Asia/Shanghai 则按北京时间）

# 管理员默认账号（首次启动自动创建）
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# 调试模式
DEBUG=True
